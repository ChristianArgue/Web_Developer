// Objeto Math

let resulado;

resulado = Math.PI;
resulado = Math.round(3.5);
resulado = Math.ceil(3.5); // Ceil siempre redondea hacia arriba
resulado = Math.floor(3.5); // Floor siempre redondea hacia abajo
resulado = Math.sqrt(144);
resulado = Math.abs(-200);
resulado = Math.min(3, 5, 1, 8, 2, 10);
resulado = Math.max(3, 5, 1, 8, 2, 10);
resulado = Math.random();
resulado = Math.floor(Math.random() * 30);

console.log(resulado);